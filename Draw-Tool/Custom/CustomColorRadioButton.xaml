<RadioButton x:Class="Draw_Tool.Custom.CustomColorRadioButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Draw_Tool.Custom"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <RadioButton.Template>
        <ControlTemplate>
            <Grid>
                <ToggleButton IsChecked="{Binding IsChecked,RelativeSource={RelativeSource TemplatedParent}}"/>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="2*"/>
                        <RowDefinition Height="1*"/>
                    </Grid.RowDefinitions>
                    <Ellipse Width="200" Height="200" Fill="{Binding InkColor,RelativeSource={RelativeSource TemplatedParent}}"/>
                    <TextBlock Grid.Row="1" Text="RED" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{Binding InkColor,RelativeSource={RelativeSource TemplatedParent}}"/>
                </Grid>
            </Grid>
        </ControlTemplate>
    </RadioButton.Template>
</RadioButton>
